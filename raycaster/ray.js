class Ray {
    constructor(origin, direction, distance) {
        this.origin = origin;
        this.direction = direction;
        this.distance = distance;
    }
    // any point p = origin + raydirection * time (or distance)
}